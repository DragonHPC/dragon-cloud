questions:
- variable: backend.nnodes
  description: The number of backend Dragon compute nodes. This should be in the range of 1 to n, where n is the total worker nodes in your Kubernetes cluster.
  type: int
  required: true
  label: Num Compute nodes
  min: 1
- variable: python.packages
  description: List of Python packages to be installed via pip.
  type: listofstrings
  label: Python Packages
